#include<stdio.h>
#include<conio.h>
int main()
{
int a[20],i,sum=0;
float avg;
printf("Enter the array elements");
for(i=0;i<10;i++)
{
scanf("%d",&a[i]);
}
for(i=0;i<n;i++)
{
sum=sum+a[i];
avg=sum/10;
}
printf("Average element is %d",avg);
return 0;
}
